# IAM policies are managed within the iam/roles module (terraform-aws-iam-ecs).
# This directory is reserved for any additional standalone policies if needed in the future.
#
# Currently no standalone policy resources are required.
# All ECS execution and task policies are co-located with the roles in ../roles.
